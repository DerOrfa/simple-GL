#export CC=distcc gcc
#export CXX=distcc g++
export MAKEFILE = Makefile.manual
export CXXFLAGS= -I/opt/prog/eclasses/eclasses2 -fPIC -g3 -DDATA_DIR="\"./data\"" -DGL_GLEXT_PROTOTYPES -pthread
export CFLAGS=$(CXXFLAGS)


%:
	${MAKE} $@ -f Makefile.manual

#$(shell sdl-config --cflags) 
#$(shell sdl-config --libs)
#export LIBS = -L$(EMATH_DIR)
#export LINK_LIBS = $(LIBS) -lm -lGL -lGLU -leclasses2 -lIL -lILUT -lILU 
